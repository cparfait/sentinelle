# Dependances de developpement (tests, lint). Production : requirements.txt seul.
pytest>=8
pytest-cov>=5
ruff>=0.8

# Reprise des donnees de SoftInventory (tools/reprise_softinventory.py) : lecture
# de sa base PostgreSQL. Outillage de migration, pas une dependance de l'app --
# Sentinelle tourne sur SQLite et n'a jamais besoin de ce pilote a l'execution.
psycopg[binary]
